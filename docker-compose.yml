services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/server/data
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: always

  caddy:
    image: caddy:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

volumes:
  caddy_data:
  caddy_config:

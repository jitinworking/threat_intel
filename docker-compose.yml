services:
  app:
    build: .
    ports:
      - "3001:3001"
      - "5173:5173"
    volumes:
      - ./data:/app/server/data
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: always
